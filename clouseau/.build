#!/usr/bin/env sh

VERSION="1.0.0"
TAG="clouseau-${VERSION}"

GROUP=jataware
NAME=dojo
IMAGE="${GROUP}/${NAME}"

docker build --no-cache -f cato/Dockerfile \
       --build-arg CLOUSEAU_VERSION="${VERSION}" \
       -t "${IMAGE}:${TAG}" \
       .
