import React from 'react';
import SubmitSuccessPage from '../client/datasets/SubmitSuccessPage';

export default {
  title: 'Dataset Registration/Submit Success',
  component: SubmitSuccessPage,
};

const Template = (args) => (
    <SubmitSuccessPage {...args} />
);

export const Basic = {
  args: {
  }
};

